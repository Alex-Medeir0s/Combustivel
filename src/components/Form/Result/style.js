import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    resultContainer: {
        marginTop: 20,
        alignItems: "center"
    },

    resultText: {
        color: "#0D47A1",
        fontSize: 20,
        fontWeight: "bold"
    },

    resultValue: {
        color: "#1976D2",
        fontSize: 22,
        fontWeight: "bold"
    }
});

export default styles;
